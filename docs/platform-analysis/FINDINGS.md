# 결함·위험·검증 과제

2026-09-24 · 로컬 작업 트리 기준. A = admin, F = storefront. `재현`은 실제 함수를 가짜 데이터로 실행한 결과, `정적 확인`은 코드 경로의 근거, `후보`는 운영 조건 확인이 더 필요한 항목이다. P1은 해당 경로 운영 전 우선 보완, P2는 측정과 운영 영향에 따라 개선한다. 실제 운영 사고가 발생했다고 단정하지 않는다.

## 1. 기존 물류 검토의 재사용

상세 내용은 [2026-09-24 물류 보고서](2026-09-24-fulfillment-review.md). 이번에 F01/F02를 실제 함수로 다시 재현했다.

| ID | 우선순위·증거 | 문제 | 후속 완료 조건 |
| --- | --- | --- | --- |
| F01 | P1 · 재현 | A 송장 발급 후 B 입고 시 A+B가 다시 합포장되어 A 전용 송장을 상속 | 송장 발급 후 출고 묶음 고정, 신규 입고는 미배정 그룹에만 반영 |
| F02 | P1 · 재현 | `Artist A`/`Artist B`의 코드가 모두 `1FS8`; 송장·완료 상태 충돌 | 불변 작가/출고 ID와 외부 짧은 번호의 유일성 검증 |
| F03 | P1 · 정적 확인 | 출고 완료 기록 이후 로컬 갱신 실패 시 재시도가 나머지 갱신을 생략 | 실패 주입 후 재실행으로 전체 상태 수렴 |
| F04 | P1 · 정적 확인 | API가 received 품목만 선택해 처리하고 전체 필수 수량 준비를 재검증하지 않음 | 서버가 출고 묶음·필요 수량·미처리 잔량을 검증 |
| F05 | P2 · 정적 확인 | 주문 수량 2개에 1개씩 입고하면 누적 입고 완료로 연결되지 않는 경로 | 라인별 누적 입고·배정·출고 수량 보존 |

## 2. 이번 전체 분석에서 추가한 항목

### PF01 — 구매 시점 대신 웹훅 수신 당시 정산 단가 선택

- **P1 / 정적 확인 / R06.** A [주문 웹훅](../../../blank-seoul-admin/app/api/shopify/webhook/orders/route.ts) 45·85·106·141줄.
- 최신 `master_products.price` 또는 옵션 단가를 읽어 `artist_orders.settlement_price`로 저장한다. 이 경로에는 주문 기준 시각에 유효한 가격 버전을 선택하는 로직이 없다.
- 구매→가격 수정→웹훅 지연 도착 순서라면 요구한 과거 단가와 달라질 수 있다. 저장 이후 스냅샷 사용만으로 이 문제는 해결되지 않는다.
- **조치:** 가격 버전·적용 시각·구매 기준 사건 확정. 과거 단가 미확인 시 정산 보류. 가격 변경 전후·지연·중복·수정 주문 시험.

### PF02 — 단가 누락·취소 상태를 정산 완료 처리에서 차단하지 않음

- **P1 / 정적 확인 / R06, A04.** A [정산 완료 PATCH](../../../blank-seoul-admin/app/api/admin/orders/route.ts) 197·224·229줄.
- 입력 ID 중 `payment_status=unpaid`만 선택하고, 금액 계산은 `(settlement_price || 0) * (quantity || 1)`이다. 이 경로에서 단가 null이나 주문 취소/지급 대상 여부를 검증하지 않고 paid로 저장할 수 있다.
- 따라서 화면이 정상 대상만 선택한다는 전제에 의존한다. 실제 지급 대상 정책은 미결정이나 누락 단가를 0원 지급 완료로 처리하는 것은 R06 보류 원칙과 충돌한다.
- **조치:** 서버의 지급 대상·단가·수량 검증, 조건부 상태 변경·원자적 마감, 누락 단가/취소/동시 환불/중복 요청 시험. 실제 이체 성공과 완료 표시를 구분.

### PF03 — 환불·정산 취소가 기존 거래 근거를 변경

- **P1 / 정적 확인 및 설계 보완 / R05–R06, A04.** A [환불 SQL](../../../blank-seoul-admin/supabase/migrations/20260923_hardening_refunds.sql) 44줄, `api/admin/orders/route.ts` 174줄.
- 부분 환불은 `artist_orders.quantity`를 줄인다. `processed_refunds`에는 ID·주문·시각만 저장하고 환불 라인 수량·금액은 보존하지 않는다. 정산 취소는 `paid_at/settled_amount/settled_tax_type`를 null로 지운다.
- 외부 Shopify 이력이 남더라도 내부 원거래·지급·조정의 독립적인 대조 근거가 부족할 수 있다. 운영 DB 감사 트리거 등 별도 보존 장치는 미확인이다.
- **조치:** 원주문 수량과 환불/정산 조정 원장을 분리하고 지급 취소 이력을 남긴다. 기지급 부분 환불·반복 환불·기간별 잔액 대조 시험. 구체적인 차감 정책은 사용자와 확정.

### PF04 — 취소 웹훅이 DB 갱신 실패 후에도 성공 응답

- **P1 / 정적 확인 / R05, A03.** A [취소 웹훅](../../../blank-seoul-admin/app/api/shopify/webhook/orders-cancel/route.ts) 58·85·95·106줄.
- 품목 갱신 오류는 로그만 남기며 계속 진행한다. 부모 주문 갱신의 반환 오류도 확인하지 않고 마지막에 200을 반환한다.
- 외부에는 처리가 완료된 것처럼 보이지만 일부 품목은 출고 가능 상태로 남을 수 있다. 별도 대조 작업 유무·운영 발생 여부는 미확인이다.
- **조치:** 영속적인 수신 기록과 재처리, DB 변경 트랜잭션, 실패 전파, 부분 실패 후 반복 수신 시험.

### PF05 — 처리 중 종료된 동기화 작업의 회수 경로 부족

- **P1 / 정적 확인·운영 보완 여부 미확인 / R03, A03.** A `lib/queue/syncQueue.ts:85`, `shopifySyncWorker.ts:63`, `supabase/migrations/20260913_scale_10k_phase2_sync_queue.sql:42`.
- 작업 claim은 `pending`을 선택해 `processing`으로 바꾼다. 읽은 RPC와 fallback 모두 다음 claim에서 만료된 processing 작업을 선택하지 않는다. 워커의 외부 호출 중 프로세스가 종료되면 catch/fail 함수가 실행되지 않을 수 있다.
- 별도 워커 리스의 만료는 작업 행의 상태 복구와 다르다. 검색한 `app/lib/supabase` 소스에서는 만료 processing 회수 경로를 확인하지 못했다. 운영 DB 예약 작업 존재 여부는 미확인이다.
- **조치:** 작업 임대 만료 후 재획득·오래된 워커 결과 거부·재시도 상한·실패 큐를 정의. claim 직후 종료 후 재시작 시험.

### PF06 — 주문 재수집이 기존 처리 상태와 메타데이터를 덮어쓸 수 있음

- **P1 / 정적 확인·호출 조건 추가 검증 / R03/R12.** A `lib/pipeline/modules/order-collector/index.ts:102`, `lib/pipeline/shared/order-manager.ts:35`·78.
- 수집된 정상 주문을 항상 `sync_status:PENDING`, `raw_data:null`로 upsert한다. 이미 우체국 접수됐으나 아직 Shopify 미배송 상태인 주문이 다시 수집될 경우 기존 상태·실측/접수 메타데이터를 덮어쓸 수 있다.
- **조치:** 외부 주문 데이터 갱신과 내부 처리 상태 변경을 분리. 동일 주문 재수집·부분 배송·재접수 방지 시험. 실제 주기적 호출 여부도 확인.

### PF07 — 전체 조회와 인메모리 캐시에 의존하는 물류 경로

- **P2 / 성능·정합성 위험 후보 / R07/R09/R10.** A `api/3pl/orders/route.ts:32`, `lib/pipeline/shared/product-store.ts:31`, `lib/supabase/stock.ts:35`, `api/artist/settlements/route.ts:38`.
- 창고 주문 목록 및 상품 목록을 페이지 범위 없이 읽고, SKU fallback은 옵션 보유 상품 목록을 순회한다. 일부 정산 조회도 누적 이력을 일괄 가져온다.
- 수량이 늘면 조회 비용과 응답이 커질 수 있고, 서버의 행 반환 제한에 걸리면 누락 가능성이 있다. 60초 프로세스 캐시는 모든 서버 인스턴스가 공유하는 캐시라는 증거가 아니다. IO 경고의 실제 원인으로 확정하지 않는다.
- **조치:** 대상 데이터량·실행 계획·행 수 제한·IO 측정 후 페이지네이션, SKU 조회 인덱스/정규화, 집계·캐시 전략 결정.

### PF08 — 상품 대조 GET 요청이 잘못된 삭제를 수행할 수 있음

- **P1 / 정적 확인 / R03, A08.** A [상품 대조 API](../../../blank-seoul-admin/app/api/admin/reconcile-shopify/route.ts) 62·75·81줄.
- 제목으로 Shopify ID를 찾아 DB에 새로 연결한 뒤, 삭제 판단은 갱신 전 `masterProducts` 배열로 만든 ID 집합을 사용한다. 처음에 DB ID가 없었던 상품은 방금 연결했어도 집합에 없어 삭제 후보가 될 수 있다.
- 이 GET 경로는 관리자 인증이 있으나 실제 외부 상품 삭제를 수행한다. 이번 조사에서 호출하지 않았다.
- **조치:** 변경 전용 API·dry-run·대상 목록 검토·갱신된 ID 집합·페이지 완전성 확인. “ID 없음/제목 일치/새로 연결” 사례에서 삭제하지 않는 회귀 시험.

### PF09 — 예약 작업과 운영 환경 적용 상태가 불명확

- **P2 / 운영 미검증 / R03/R09/R10.** F `vercel.json`의 `crons`는 빈 배열. A `.github/workflows/fulfillment-pipeline.yml`은 schedule 주석, job `if:false`.
- 관련 cron 엔드포인트가 존재하지만 운영에서 어떤 주기로 누가 호출하는지 증거가 없다. 외부 스케줄러가 있을 수 있으므로 “운영 작업이 없다”고 단정하지 않는다.
- **조치:** 작업별 스케줄러·인증·마지막 성공·중복 실행·실패 경보·최대 적체 시간을 기록. 운영 자료 필요.

### PF10 — 물류 준비 판단을 통합해야 하는 별도 자동 접수 경로

- **P1 조건부 위험 / 정적 경로 확인 / R12.** A `api/cron/auto-pipeline/route.ts:26`·59·79.
- `settings.auto_pipeline`이 켜지면 Shopify 미배송 주문과 EMS 이력으로 대상을 만들고 `runEmsSubmitter`를 호출한다. 이 핸들러는 포털의 `groupArtistOrdersIntoSubOrders`나 주문별 received 검사를 호출하지 않는다.
- 현재 설정값·운영 실행 여부·하위 제출기의 전체 방어 조건은 미검증이다. 활성화 전 준비 검증이 동일하게 적용되는지 확인해야 한다.
- **조치:** UI·batch·cron에서 공통 출고 묶음/입고 검증 사용. 미입고 주문의 자동 접수 차단 시험.

### PF11 — 두 저장소 자동화는 현재 실행기에서 제한

- **P2 / 정적 확인 / R08.** F `scripts/agent-bridge-front.mjs:362`·369 및 양쪽 `.agent-bridge/pipeline.config.json`.
- 제공된 실행기는 시작 디렉터리가 Git 루트인지 확인하고 workspaceRoot가 그 루트 밖이면 거부한다. 문서만으로 두 프로젝트 수정·검증이 활성화되지 않는다.
- **조치:** 두 루트의 명시적 범위, 저장소별 기준선·diff·테스트, 동시 변경 보호, 수동 SQL/Git push 경계를 함께 구현·검증.

## 3. 추가 확인 사항 — 결함으로 확정하지 않은 항목

- orders/create 핸들러가 미결제 주문을 무시하므로 이후 결제 완료 이벤트의 실제 구독·처리 경로를 확인해야 한다.
- `settlement_price`의 주문 충돌 키가 SKU/옵션 제목 기반이다. 동일 SKU 중복 라인, null 옵션, 주문 편집과 재전송의 실제 DB 유일성 제약을 대조해야 한다.
- 두 프로젝트의 API 버전 문자열·배포 Next.js 버전·SQL 적용 시점이 다를 수 있다. 지원 여부는 실제 운영 버전과 공식 문서로 후속 확인한다.
- 반품 주소 필드의 존재를 실제 반품 라벨·수령·검수 프로세스 완료로 간주하지 않는다.
- 물류 입고 상태 변경과 물리 수량 변경, 큐 적재와 상품 저장의 전체 원자성은 별도 장애 주입 검증이 필요하다.
- 모든 API의 인증, RLS/Storage 정책, 외부 웹훅·업로드·레이트리밋을 전수 보안 감사한 것은 아니다.

결함 해결 후에는 삭제 대신 해결 상태·변경 경로·검증 ID를 추가한다. 사업 정책을 새로 정해야 하는 내용은 목표 문서의 결정 기록과 연결한다.
